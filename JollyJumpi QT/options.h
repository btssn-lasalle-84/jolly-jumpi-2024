#ifndef OPTIONS_H
#define OPTIONS_H

#include <QObject>
#include <QList>

class Options : public QMainWindow
{
    Q_OBJECT

    public:
        explicit Options(QWidget *parent = nullptr);
        ~Options();

    private:

};

#endif // OPTIONS_H
